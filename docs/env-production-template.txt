# =============================================================================
# PALAUTESEINÄ (FEEDBACK WALL) - COOLIFY ENVIRONMENT VARIABLES
# =============================================================================
# Aseta nämä muuttujat Coolify UI:ssa: Settings → Environment Variables
#
# HUOM: Coolify generoi automaattisesti seuraavat:
#   - SERVICE_FQDN_APP (domain/URL)
#   - SERVICE_USER_POSTGRES (PostgreSQL käyttäjä)
#   - SERVICE_PASSWORD_POSTGRES (PostgreSQL salasana)
#   - SERVICE_PASSWORD_REDIS (Redis salasana)
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION (PAKOLLINEN)
# -----------------------------------------------------------------------------
APP_NAME="Palauteseinä"
APP_ENV=production
APP_DEBUG=false

# Generoi: php artisan key:generate --show
APP_KEY=base64:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# -----------------------------------------------------------------------------
# LOGGING
# -----------------------------------------------------------------------------
LOG_CHANNEL=stderr
LOG_LEVEL=warning

# -----------------------------------------------------------------------------
# ADMIN & MODERATION (PAKOLLINEN)
# -----------------------------------------------------------------------------
# Pilkuilla erotettu lista admin-sähköposteista (Google OAuth)
ADMIN_EMAILS=admin@example.com,teacher@example.com

# Ulkoinen moderointipalvelun URL (valinnainen)
# MODERATION_SERVICE_URL=https://moderation-api.example.com

# -----------------------------------------------------------------------------
# GOOGLE OAUTH (PAKOLLINEN kirjautumiseen)
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=xxxxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxx

# -----------------------------------------------------------------------------
# MAIL (valinnainen)
# -----------------------------------------------------------------------------
# Oletuksena log-mailer (ei lähetä oikeita sähköposteja)
MAIL_MAILER=log

# SMTP-asetukset jos haluat lähettää sähköposteja:
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your-smtp-user
# MAIL_PASSWORD=your-smtp-password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_FROM_NAME="Palauteseinä"

# -----------------------------------------------------------------------------
# TELESCOPE (valinnainen)
# -----------------------------------------------------------------------------
TELESCOPE_ENABLED=true

# -----------------------------------------------------------------------------
# DEPLOYMENT FLAGS
# -----------------------------------------------------------------------------
# Aseta true ensimmäisellä deploylla, sitten false
RUN_MIGRATIONS=false

# =============================================================================
# ENSIMMÄINEN DEPLOY - OHJE
# =============================================================================
# 1. Lisää GitHub repo Coolifyyn
# 2. Valitse Docker Compose
# 3. Aseta Environment Variables (yllä olevat)
# 4. Aseta RUN_MIGRATIONS=true
# 5. Deploy
# 6. Kun toimii, aseta RUN_MIGRATIONS=false
# 7. Konfiguroi domain Coolify UI:ssa → Settings → Domains
